# NDPi LCD Display Monitor - Configuration File
# 
# Specify the full paths to your data files below
# The script will read from these files and display their contents
#
# Example format:
#   device_name_path=/path/to/file.txt
#
# Comments start with #

# Device name (e.g., "NDPi Client" or "Living Room Screen")
device_name_path=/home/ndpi-client/ndpi/tmp/device_name.txt

# Device IP address (e.g., "192.168.1.100")
device_ip_path=/home/ndpi-client/ndpi/tmp/device_ip.txt

# NDI Status (e.g., "receiving", "idle", "error")
ndi_status_path=/home/ndpi-client/ndpi/tmp/ndi_status.txt

# NDI Source Resolution (e.g., "1920x1080")
ndi_resolution_path=/home/ndpi-client/ndpi/tmp/ndi_resolution.txt

# Output Resolution (e.g., "1280x720")
output_resolution_path=/home/ndpi-client/ndpi/tmp/output_resolution.txt
